{
  "name": "scan-to-paste",
  "version": "0.0.0",
  "description": "scan a QR code, get connected and paste the content to the other one",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "*",
    "socket.io": "*"
  }
}
